# dsh-encoding-guard

DSH 文件工具的编码守卫：让模型在文本读写中把一切文本文件视为 UTF-8 no BOM，会话结束后磁盘回到原编码。

## Language

### 保障边界

**绕过（Bypass）**:
模型经某通道读出或写入了与其 UTF-8 视图不一致的字节序列的路径。判据是"模型观察到磁盘原始编码字节"或"非 UTF-8 字节未经转换落盘"，而非"通道未被转换桥拦截"。
_Avoid_: 漏网、未覆盖（未定义判据的泛称）

**编码无关通道（Encoding-agnostic channel）**:
只接触文件路径或纯二进制内容的通道（如 glob、read_image）。无论文件编码为何，其行为与结果不变；它不是绕过路径。
_Avoid_: 把这类通道列入"绕过清单"

**实用一致（Practical consistency）**:
本项目承诺的保证级别：read/write/edit 完全正确；其余模型可见通道保证 ASCII 模式对 GBK 系文件可命中（UTF-16 文件 grep 任何模式均漏配，由 eb_grep 与 read 自愈补位）、有使用指导、经 read 触达即可自愈；不承诺进程外通道（shell 等）透明。
_Avoid_: 强一致（本项目明确不追求"所有通道字节级一致"）

### 桥接机制

**转换桥（Transcode bridge）**:
读写工具执行前把非 UTF-8 no BOM 文本文件原地转为 UTF-8 no BOM 的机制；字符序列与行尾在往返转换中保持不变。
_Avoid_: 编码转换器（泛称，丢失"桥接官方工具"含义）

**账本（Ledger）**:
登记"已转为 UTF-8、待恢复原编码"文件及其原编码的登记簿；恢复完成后条目消账。
_Avoid_: 缓存、队列

**自愈（Self-healing）**:
并发触达或跨轮恢复导致状态过期时，通过既定的版本过期报错与重新读取回到一致性的已定义行为，而非待修缺陷。
_Avoid_: 竞态 bug（它是设计语义，不是缺陷）

### 读路径与转换形态

**内存解码（In-memory decode）**:
只读补位工具（eb_grep / eb_peek）的实现形态：读字节 → 检测 → 解码 → 返回结果；不写磁盘、不进账本。
_Avoid_: 虚拟转换、临时桥（看似走磁盘转换桥的说法）

**会话视图（Session view）**:
文件在会话轮次内于磁盘上呈现 UTF-8 no BOM、轮末恢复原编码的默认不变量；eb_convert 默认遵循它。
_Avoid_: 临时状态、草稿态

**持久转换（Persist）**:
eb_convert 的显式参数语义：转换结果在轮末不恢复，文件从此就是 UTF-8 no BOM；属迁移语义，需显式承担。
_Avoid_: 硬转换、强制转换
